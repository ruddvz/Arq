# Command system

This directory specifies 107 commands.

Every authoring tool uses the same lifecycle, typed-operation, local-durability,
validation, undo, recovery, accessibility and performance contract.
