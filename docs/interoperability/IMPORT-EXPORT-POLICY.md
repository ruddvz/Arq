# Import and export policy

Every import and export creates a report.

Imports are temporary transactions and do not partially mutate the project on failure.

Support claims are published by format, version, category and operation.

Never say full support when only a subset is tested.
