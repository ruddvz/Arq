# Permission-denied template

**[Action] requires [Role] access**

Your current role is [Current role]. You can still [allowed actions relevant to context].

Primary action: **[Non-destructive next action]**

Do not use this template if the actual blocker is:

- capability not implemented;
- offline state;
- validation;
- missing configuration.
