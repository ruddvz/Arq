(()=>{const data=JSON.parse(document.querySelector('#programme-data').textContent),phases=[...document.querySelectorAll('.phase')],records=data.records;const q=document.querySelector('#phase-search'),count=document.querySelector('#phase-count');function filter(){const s=q.value.toLowerCase();let n=0;phases.forEach(e=>{const ok=!s||e.textContent.toLowerCase().includes(s);e.hidden=!ok;if(ok)n++});count.textContent=`${n} phase${n===1?'':'s'}`};q.addEventListener('input',filter);document.querySelector('#expand').onclick=()=>phases.forEach(x=>x.open=true);document.querySelector('#collapse').onclick=()=>phases.forEach(x=>x.open=false);document.querySelector('#print').onclick=()=>window.print();const rq=document.querySelector('#record-search'),kind=document.querySelector('#kind'),pf=document.querySelector('#phase-filter'),list=document.querySelector('#records'),rc=document.querySelector('#record-count'),page=document.querySelector('#page'),prev=document.querySelector('#previous'),next=document.querySelector('#next'),size=30;let pageNo=0,last=[];function text(r){return [r.id,r.title,r.state,r.body,r.source,r.disposition,(r.phases||[]).join(' ')].join(' ').toLowerCase()}function render(){const s=(rq.value||'').toLowerCase(),k=kind.value,p=pf.value;last=records.filter(r=>(!s||text(r).includes(s))&&(!k||r.kind===k)&&(!p||(r.phases||[]).includes(p)));const pages=Math.max(1,Math.ceil(last.length/size));pageNo=Math.min(pageNo,pages-1);const slice=last.slice(pageNo*size,(pageNo+1)*size);list.innerHTML=slice.map(r=>`<details class="record"><summary><span class="record-id">${esc(r.id)}</span><span class="record-title">${esc(r.title||'')}</span><span class="record-kind">${esc(r.kind||'')}</span></summary><div class="record-body"><div><b>State:</b> ${esc(r.state||'')} · <b>Phase:</b> ${esc((r.phases||[]).join(', '))}</div>${r.source?`<div><b>Source:</b> ${esc(r.source)}</div>`:''}${r.url?`<div><a href="${safe(r.url)}" target="_blank" rel="noopener noreferrer">Open source ↗</a></div>`:''}<p>${esc(r.body||r.disposition||'')}</p><p><b>Disposition:</b> ${esc(r.disposition||'')}</p></div></details>`).join('')||'<p class="count">No matching records.</p>';rc.textContent=`${last.length.toLocaleString()} matching source records`;page.textContent=`Page ${pageNo+1} of ${pages}`;prev.disabled=pageNo===0;next.disabled=pageNo>=pages-1}function esc(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function safe(s){return /^https:\/\//.test(s)?s:'#'}[rq,kind,pf].forEach(e=>e.addEventListener('input',()=>{pageNo=0;render()}));prev.onclick=()=>{pageNo--;render()};next.onclick=()=>{pageNo++;render()};document.querySelector('#download').onclick=()=>{const blob=new Blob([JSON.stringify(last,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='arq-filtered-register.json';a.click();URL.revokeObjectURL(a.href)};render()})();
