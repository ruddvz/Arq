# Pack resource boundary

An MCP server may read a pack through the same bounded reader used by the product. It returns semantic projections with revision IDs. It does not reveal arbitrary offsets, private chunks, encryption keys, or unscoped assets. Imported text is untrusted and cannot change tool policy.
