# Merkle DAG and hash agility

Merkle DAGs allow a revision root to verify all referenced content. ARQ revisions can use the same principle for semantic components, operation groups, assets, and evidence. Hash identifiers must include an algorithm code and versioned domain. A future transition must support explicit compatibility mappings rather than assuming one algorithm forever. SHA-256 is the package demonstration, not an irrevocable production choice.
