# Collaboration conflict research

Generic CRDTs are useful for presence, comments, text, and some unordered metadata. They are unsafe as an automatic answer for constrained geometry, assembly mates, feature order, and hosted relationships. ARQ should use semantic operation merge with read sets, write sets, preconditions, deterministic validation, and explicit conflicts. CRDTs may be used inside carefully bounded field types.
