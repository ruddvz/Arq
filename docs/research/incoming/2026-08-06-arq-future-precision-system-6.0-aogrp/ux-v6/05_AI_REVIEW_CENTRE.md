# AI Review Centre UX

Show request, assumptions, grant, base revision, operations, affected objects, documents, geometry preview, validation, analysis evidence, warnings, approval digest, commit result, provenance, and grouped undo. AI cannot approve its own proposal.
