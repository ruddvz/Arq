# Recovery and repair UX

Show which recovery root was used, which generation opened, whether trailing unreachable data was ignored, what remains safe, and whether a compact repaired copy can be created. Repair always writes a new file.
