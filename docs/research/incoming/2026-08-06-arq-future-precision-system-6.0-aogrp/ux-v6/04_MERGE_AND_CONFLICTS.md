# Merge and conflict UX

Conflicts show affected semantic objects, both operations, dependencies, violated constraints, preview alternatives, and safe resolutions. Generic “sync conflict” dialogs are insufficient.
