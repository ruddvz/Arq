# Capabilities and domain packs

Files declare required and optional capabilities. Domain packs register types, operations, validators, UI descriptors, import/export mappings, and migrations. Packs execute in bounded sandboxes and cannot access unrelated project or network resources. Unknown optional payloads remain byte-preserved.
