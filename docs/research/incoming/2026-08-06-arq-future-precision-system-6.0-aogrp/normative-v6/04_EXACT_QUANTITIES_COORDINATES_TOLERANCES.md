# Exact quantities, coordinates, and tolerances

Canonical dimensions use integer coefficient, decimal scale, and registered unit. Coordinates use explicit frames. Global geospatial coordinates and local engineering coordinates are separate. Tolerance is an explicit policy, not a hidden kernel epsilon. Floating-point approximations are derived and record error bounds.
