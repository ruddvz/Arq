# Persistent topology selection

Permanent references MUST NOT use transient kernel indexes. Selectors combine owning entity, feature lineage, role, geometry signature, adjacency, orientation, and dimensional relationships. Resolution states are Exact, Rebound, Ambiguous, Missing, Invalidated, and Unsupported. Required ambiguous selections block the dependent operation.
