# MCP and automation harness

MCP resources expose revision-pinned semantic views and evidence, not raw pack offsets. Tools create bounded proposals and tasks. The host validates grants, base revision, operation schemas, assumptions, limits, proposal digest, approval, and invariants before commit. Long-running geometry, import, and simulation tasks write isolated artefacts until accepted.
