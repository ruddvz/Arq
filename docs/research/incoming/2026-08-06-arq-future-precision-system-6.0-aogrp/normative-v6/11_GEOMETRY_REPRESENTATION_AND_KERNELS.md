# Geometry representation and kernels

Canonical ARQ objects store semantic parameters, feature recipes, constraints, selectors, units, tolerance, and derivation provenance. Kernel payloads identify kernel family, version, codec, tolerance, source recipe digest, and validity evidence. A missing kernel payload triggers regeneration or reference-only mode, not semantic project loss.
