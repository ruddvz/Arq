# Federation, partial clone, and LOD

A root project may pin child project UUID, revision ID, transform, coordinate frame, expected root, loading policy, and permission scope. Sparse manifests declare omitted segments and trusted retrieval hints. LOD packs are derived. Missing federated content must not be mistaken for deletion.
