# Conformance and independent readers

Release requires golden byte vectors, healthy and damaged files, two independent reader implementations, deterministic object-ID agreement, crash-injection results, hostile-input fuzzing, migration corpus, and browser evidence. The package Python reader is only one demonstration.
