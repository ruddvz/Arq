# MCP Apps and review UI

A future MCP App may render proposal previews and diagnostics, but the ARQ host remains the approval authority. The app receives a read-only review model. It cannot modify proposal bytes, mint approval tokens, or represent solver output as professional approval.
