# Grants and authorization

The host creates a short-lived grant bound to issuer, audience, subject, client, project, base revision, resources, operation types, object or spatial scope, byte and operation limits, remote-compute rights, export rights, expiry, and nonce. The authorization server issuer is validated. Credentials are not reusable across issuers.
