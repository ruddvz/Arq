# MCP conformance matrix

Test protocol revision, discovery, deterministic catalogs, routing headers, client metadata, issuer validation, grant scope, proposal lifecycle, stale bases, mutation after approval, replay, task cancellation, MRTR fallback, resource pinning, compatibility adapters, rate limits, and structured diagnostics across every supported client and transport.
