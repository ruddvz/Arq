# Repository implementation prompt for ARQ File System 5.0

Use this package as proposed research and test material. Do not implement it literally or allocate its demo identifiers.

1. Resolve the repository, active branch, immutable HEAD, working tree, open PRs, and requested delivery stop.
2. Read `CLAUDE.md`, `.zeus/FAST-KERNEL.md`, and `.zeus/INVARIANTS.md`.
3. Recompile the task using current code, accepted ADRs, schemas, migrations, tests, CI, and runtime evidence.
4. Inventory the existing `.arq` application ID, user versions, capability registry, schema v1/v2, migration graph, recovery paths, workers, and fixtures.
5. Resolve the SQLite-WASM/OPFS versus Dexie/IndexedDB authority conflict through an ADR before adding a new open pipeline.
6. Correct the current `STATUS.md` 3D contradiction against code and tests.
7. Implement the smallest complete vertical slice: product-reachable open, isolated validation, workspace hydration, portable publication, fresh-reader verification, source-preserving failure, and truthful UI states.
8. Reuse existing systems. Do not create parallel operation, evidence, invariant, language, ADR, or release systems.
9. Add current MCP 2026-07-28 only after the file and operation boundary is product reachable. Keep protocol adapters separate from canonical domain logic.
10. Run repository gates and attach exact evidence. Stop at the requested delivery boundary. Do not branch, commit, open a PR, merge, deploy, or change settings unless explicitly requested.

Reject or revise any Version 5 proposal that conflicts with current accepted repository authority.
