# Documentation drift controls

Generate package counts, test counts, schema counts, routes, tool catalogs, capabilities, and consumer reports. Add CI checks for stale dates, superseded standards, contradictory status claims, absent referenced files, cache artefacts, unallocated demo IDs, and public claims without evidence links. `STATUS.md` should fail CI when generated facts disagree.
