# Federation and partial loading UX

The project browser shows loaded, unloaded, stale, missing, permission-blocked, and incompatible references. Editing tools reveal when required context is unloaded. Users can pin a revision, update a reference, compare changes, or keep the current pin. Large-city loading is progressive without pretending unloaded areas were validated.
