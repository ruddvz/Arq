# Interchange profile picker

Import and export start by choosing a named format edition and workflow profile. The UI shows supported concepts, expected flattening, geometry strategy, units, coordinate systems, linked files, property mapping, and validation tools. The final report is saved with the project or exported separately.
