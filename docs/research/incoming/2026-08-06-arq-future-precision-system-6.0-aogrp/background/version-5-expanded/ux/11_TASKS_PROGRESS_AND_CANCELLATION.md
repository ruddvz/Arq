# Tasks, progress, and cancellation UX

Long operations display named phase, bounded progress where measurable, elapsed time, budget, cancel behaviour, and whether partial outputs are safe. Cancellation never leaves an accepted half-operation. A task can finish with success, input required, cancelled, failed safely, or completed with warnings.
