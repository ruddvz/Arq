# Release and rollback

AOGRP remains experimental behind a feature flag. Current `.arq` publication remains available until migration evidence and product release gates pass. Rollback disables new writes but keeps preserving reads and source export. No forced in-place migration.
