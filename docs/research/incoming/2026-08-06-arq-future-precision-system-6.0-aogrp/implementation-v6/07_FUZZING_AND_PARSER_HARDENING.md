# Fuzzing and parser hardening

Fuzz bootstrap parsing, recovery roots, segment headers, manifests, codecs, canonical payloads, nested references, migration inputs, and external reference resolvers. Use differential readers, sanitizers, mutation corpora, decompression limits, and timeout budgets. Never auto-load network resources during validation.
