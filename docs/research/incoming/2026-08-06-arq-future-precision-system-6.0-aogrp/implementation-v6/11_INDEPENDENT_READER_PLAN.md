# Independent reader plan

Reader A: Rust implementation intended for product and WASM. Reader B: TypeScript or Python clean-room implementation using only the byte specification and vectors. Both must agree on preflight, root selection, manifest hashes, object IDs, semantic roots, capability states, and diagnostics.
