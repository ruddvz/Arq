# Schemas

These JSON Schemas describe the first planning version of the Arq model.

They are not yet production-stable.

Before implementation:

- approve unit representation;
- approve ID format;
- define versioning;
- add view, sheet, dimension and import schemas;
- validate cross-file references;
- create migration fixtures;
- generate TypeScript types only after ADR approval.
