name: Security template
on:
  pull_request:
  schedule: [{ cron: '0 6 * * 1' }]
jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Add secret scan, dependency scan, SBOM and licence policy checks"
