# Database migration rules

Use forward-only numbered migrations, never rewrite applied migrations, back up before destructive changes and separate project-file schema migrations from database migrations.
