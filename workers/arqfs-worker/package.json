{
  "name": "@arq/arqfs-worker",
  "private": true,
  "version": "0.0.0",
  "description": "SQLite WASM + OPFS runtime for @arq/arqfs (ADR-0024, ARQ-196) - loaded inside an Arq-owned dedicated Worker, never on the UI thread.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@arq/arqfs": "workspace:*",
    "@sqlite.org/sqlite-wasm": "3.53.0-build1"
  }
}
