---
description: Load UI/visual and accessibility modules from the start
---

Read `.zeus/FAST-KERNEL.md`. Force-load `.zeus/modules/ui-visual.md` and
`.zeus/modules/accessibility.md` from the start, even if the task text alone would not
trigger them. Route any additional modules on top, show the compact contract, then
execute:

$ARGUMENTS
