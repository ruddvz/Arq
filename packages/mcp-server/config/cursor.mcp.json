{
  "$comment": "Build the server first: pnpm --filter @arq/mcp-server build. Replace <repo> with the absolute path to this checkout. See config/README.md for what the grant file is and why deleting it withdraws access.",
  "mcpServers": {
    "arq": {
      "command": "node",
      "args": ["<repo>/packages/mcp-server/dist/arq-mcp-stdio.mjs"],
      "env": {
        "ARQ_MCP_GRANT_FILE": "<the path Arq writes when you share a project>"
      }
    }
  }
}
