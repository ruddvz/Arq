# @arq/model-context

Revision-aware context contracts for AI and automation. The package is read-only: it returns source IDs and freshness, while any change still travels through typed operations and deterministic validation.
