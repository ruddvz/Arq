# Backup and restore

Test point-in-time database recovery, object checksums, project snapshot restoration, deletion propagation and disaster recovery. The downloaded Arq archive remains an independent backup.
